<!-- src/test/resources/logback-test.xml -->
<configuration>
  <!-- Basic configuration for tests, e.g., only log errors to console -->
  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <logger name="com.savdev.rest" level="DEBUG"/>
  <!--  logs only data transmitted to and from the server: -->
  <logger name="org.apache.http.wire" level="DEBUG"/>
  <logger name="org.apache.hc.client5.http.wire" level="DEBUG"/>
  <logger name="com.github.tomakehurst.wiremock" level="DEBUG"/>

  <root level="info">
    <appender-ref ref="STDOUT"/>
  </root>
</configuration>